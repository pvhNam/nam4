<?xml version="1.0" encoding="utf-8"?>
<!-- Viên thuốc kính mờ: tint trắng mỏng + viền sáng, bo tròn hết cạnh.
     Dùng làm background của BlurView (blur lộ qua phần tint trong suốt + clip bo tròn). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="40dp" />
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/lg_glass_tint"
        android:endColor="@color/lg_glass_fill_soft" />
    <stroke
        android:width="1dp"
        android:color="@color/lg_glass_stroke" />
</shape>
