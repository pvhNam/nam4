<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout cho mỗi thumbnail media (ảnh/video) trong thanh preview trước khi gửi.
    80x80dp với nút X góc trên phải và icon ▶ ở giữa (chỉ hiện khi là video).
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="80dp"
    android:layout_height="80dp"
    android:layout_marginEnd="8dp">

    <!-- Thumbnail ảnh / frame đầu video -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp">

        <ImageView
            android:id="@+id/iv_thumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />
    </androidx.cardview.widget.CardView>

    <!-- Overlay tối (chỉ hiện khi là video) -->
    <View
        android:id="@+id/iv_play_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#55000000"
        android:visibility="gone" />

    <!-- Icon play ở giữa (chỉ hiện khi là video) -->
    <ImageView
        android:id="@+id/iv_play_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_circle_blue"
        android:padding="5dp"
        android:src="@android:drawable/ic_media_play"
        android:visibility="gone"
        app:tint="#FFFFFF" />

    <!-- Nút X xóa (góc trên phải) -->
    <ImageView
        android:id="@+id/btn_remove_thumb"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_gravity="top|end"
        android:layout_margin="2dp"
        android:background="@drawable/bg_circle_blue"
        android:padding="3dp"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:clickable="true"
        android:focusable="true"
        app:tint="#FFFFFF" />

</FrameLayout>